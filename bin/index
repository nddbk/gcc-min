#!/usr/bin/env node

var argv = require('minimist')(process.argv.slice(2));

var prepare = require('../index').prepare;

let ar = argv._ || [];

if (ar.length === 2) {
  let input = ar[0];
  let output = ar[1];
  if (input && output) {
    prepare(input, output);
  }
}
